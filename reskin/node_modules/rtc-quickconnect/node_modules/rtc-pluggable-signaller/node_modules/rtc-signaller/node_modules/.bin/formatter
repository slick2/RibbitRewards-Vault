#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../formatter/bin/formatter" "$@"
  ret=$?
else 
  node  "$basedir/../formatter/bin/formatter" "$@"
  ret=$?
fi
exit $ret
